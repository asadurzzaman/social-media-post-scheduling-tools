import { PostType } from "./PostTypeSelect";

interface PostFormMediaProps {
  postType: PostType;
}

export const PostFormMedia = () => {
  // Since we only support text posts now, this component can be simplified
  return null;
};